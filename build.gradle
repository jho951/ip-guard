plugins {
    id 'java'
}

subprojects {
    apply plugin: 'java-library'

    group = 'com.ipguard'
    version = '1.0.0'

    java {
        toolchain {
            languageVersion = JavaLanguageVersion.of(17)
        }
    }

    repositories {
        mavenCentral()
    }
}
